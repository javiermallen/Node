# Notas:
Esta es mi primera aplicación enode usando Yargs

```
Opciones:
      --help           Muestra ayuda                                  [booleano]
      --version        Muestra número de versión                      [booleano]
  -m, --multiplicando  Valor del multiplicando              [número] [requerido]
  -l, --listar         Si la bandera es true aparece en pantalla la tabla de
                       multiplicar                   [booleano] [defecto: false]
  -h, --hasta          Hasta que número se realiza la multiplicación [requerido]
  ```